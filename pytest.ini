[pytest]
testpaths = tests
pythonpath = src
markers =
    slow: long-running tests
    gpu: tests that require GPU access
    external: tests that require external repositories, data, services, or files
    model: tests that load model/tokenizer artifacts

# Third-Party Notices

- `ToolRL` is included as a submodule and is confirmed locally as Apache-2.0 via `third_party/ToolRL/LICENSE`.
- `Audio-Maestro` is included as a submodule; no local `LICENSE`, `COPYING`, or `NOTICE` file was found during audit.
- `DeSTA2.5-Audio` is included as a submodule; no local top-level `LICENSE`, `COPYING`, or `NOTICE` file was found during audit. Its `setup.py` includes an MIT classifier, and some files contain Apache-2.0 header text.
- Patch files under `patches/` preserve local modifications and should be treated as derived from their corresponding upstream projects rather than as relicensed under this repository's MIT license.
- Datasets, audio, model weights, checkpoints, generated outputs, logs, and caches are not included in this repository.
